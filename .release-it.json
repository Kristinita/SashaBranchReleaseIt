{
    "buildCommand": "changelog ${version}",
    "changelogCommand": "changelog ${version} -u ${git config --get remote.origin.url} -f -",
    "github": {
        "release": true,
        "tokenRef": "GITHUB_TOKEN"
    },
    "safeBump": false,
	"src": {
        "tagName": "st3-%s"
    }
}