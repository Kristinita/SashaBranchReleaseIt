{
    "buildCommand": "changelog ${version} -u ${repo} -f - | tee messages\/${version}.txt | tee -a CHANGELOG.MD && sed -i '\/\\\"install\\\": \\\"messages\\\/install\\.txt\\\"\/i\\\"${version}\\\": \\\"messages\\\/${version}\\.txt\\\",' messages.json && js-beautify -r messages.json",
    "github": {
        "release": true,
        "tokenRef": "GITHUB_TOKEN"
    },
	"src": {
        "tagName": "st3-%s"
    }
}