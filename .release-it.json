{
    "buildCommand": "changelog -u https:\/\/${repo.host}\/${repo.repository} -f Changelog.md && sed -i 's\/^version = .*$\/version = ${version}\/g' setup.cfg && sed -i 's\/^VERSION = \".*\"$\/VERSION = \"2.0.5\"\/g' \"erichek\/eric_config.py\" && python setup.py bdist_wheel",
    "changelogCommand": "changelog -f -",
    "github": {
        "release": true,
        "tokenRef": "GITHUB_TOKEN"
    },
    "npm": {
    	"publish": false
    },
    "safeBump": false
}
